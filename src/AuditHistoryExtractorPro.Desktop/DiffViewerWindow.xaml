<Window x:Class="AuditHistoryExtractorPro.Desktop.DiffViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Comparador de Cambios — Visual Diff"
        Height="520"
        Width="920"
        MinHeight="400"
        MinWidth="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#F9F9FB">

    <Window.Resources>
        <Style x:Key="DiffHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Foreground" Value="White" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Comparación de Valores"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#323130"
                   Margin="0,0,0,4" />

        <!-- Field name subtitle -->
        <TextBlock Grid.Row="1"
                   x:Name="FieldNameText"
                   FontSize="12"
                   Foreground="#605E5C"
                   Margin="0,0,0,12" />

        <!-- Side-by-side diff panels -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Old Value (Left) -->
            <Border Grid.Column="0"
                    BorderBrush="#E1DFDD"
                    BorderThickness="1"
                    CornerRadius="6"
                    ClipToBounds="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#C62828" CornerRadius="6,6,0,0">
                        <TextBlock Text="⊖  Valor Anterior (OldValue)"
                                   Style="{StaticResource DiffHeaderStyle}" />
                    </Border>
                    <RichTextBox x:Name="OldValueBox"
                                 Grid.Row="1"
                                 IsReadOnly="True"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 BorderThickness="0"
                                 Padding="8"
                                 FontFamily="Cascadia Code, Consolas, Courier New"
                                 FontSize="13"
                                 Background="White" />
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                          Width="8"
                          HorizontalAlignment="Stretch"
                          Background="Transparent" />

            <!-- New Value (Right) -->
            <Border Grid.Column="2"
                    BorderBrush="#E1DFDD"
                    BorderThickness="1"
                    CornerRadius="6"
                    ClipToBounds="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#2E7D32" CornerRadius="6,6,0,0">
                        <TextBlock Text="⊕  Valor Nuevo (NewValue)"
                                   Style="{StaticResource DiffHeaderStyle}" />
                    </Border>
                    <RichTextBox x:Name="NewValueBox"
                                 Grid.Row="1"
                                 IsReadOnly="True"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 BorderThickness="0"
                                 Padding="8"
                                 FontFamily="Cascadia Code, Consolas, Courier New"
                                 FontSize="13"
                                 Background="White" />
                </Grid>
            </Border>
        </Grid>

        <!-- Close button -->
        <Button Grid.Row="3"
                Content="Cerrar"
                HorizontalAlignment="Right"
                Padding="24,8"
                Margin="0,12,0,0"
                Click="CloseButton_Click"
                Background="#00A4EF"
                Foreground="White"
                FontWeight="SemiBold"
                BorderThickness="0"
                Cursor="Hand" />
    </Grid>
</Window>
