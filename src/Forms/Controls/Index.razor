@page "/"
@inject NavigationManager Navigation

<PageTitle>Dashboard - Audit History Extractor Pro</PageTitle>

<h1 style="color: #0078D4; margin-bottom: 30px; font-size: 36px;">📊 Panel de Control</h1>

<div class="grid">
    <div class="card" style="background: linear-gradient(135deg, #0078D4 0%, #004578 100%);">
        <h2>@_totalExtractions</h2>
        <p>Total Extracciones</p>
    </div>
    
    <div class="card" style="background: linear-gradient(135deg, #107C10 0%, #0E6B0E 100%);">
        <h2>@FormatNumber(_totalRecords)</h2>
        <p>Registros Extraídos</p>
    </div>
    
    <div class="card" style="background: linear-gradient(135deg, #00B7C3 0%, #00A0AC 100%);">
        <h2>@_activeEntities</h2>
        <p>Entidades Activas</p>
    </div>
    
    <div class="card" style="background: linear-gradient(135deg, #5C2D91 0%, #4B2579 100%);">
        <h2>@_lastExtractionDate</h2>
        <p>Última Extracción</p>
    </div>
</div>

<div style="margin-top: 40px;">
    <h2 style="color: #323130; margin-bottom: 20px;">🚀 Acciones Rápidas</h2>
    
    <div style="display: flex; gap: 15px; flex-wrap: wrap;">
        <button class="btn" @onclick="NavigateToExtract">🔽 Nueva Extracción</button>
        <button class="btn" style="background: #107C10;" @onclick="NavigateToExport">📊 Ver Exportaciones</button>
        <button class="btn" style="background: #5C2D91;" @onclick="NavigateToSettings">⚙️ Configuración</button>
        <button class="btn" style="background: #00B7C3;" @onclick="NavigateToHistory">📝 Historial</button>
    </div>
</div>

<div style="margin-top: 40px; padding: 30px; background: #F3F2F1; border-radius: 15px;">
    <h3 style="color: #0078D4; margin-bottom: 20px;">✅ Estado del Sistema</h3>
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px;">
        <div>
            <strong style="color: #107C10;">🟢 Conexión</strong><br/>
            <span style="color: #605E5C;">Dataverse Conectado</span>
        </div>
        <div>
            <strong style="color: #107C10;">🟢 Servicios</strong><br/>
            <span style="color: #605E5C;">Todos operativos</span>
        </div>
        <div>
            <strong style="color: #0078D4;">🔵 Versión</strong><br/>
            <span style="color: #605E5C;">1.0.0</span>
        </div>
    </div>
</div>

@code {
    private int _totalExtractions = 0;
    private long _totalRecords = 0;
    private int _activeEntities = 0;
    private string _lastExtractionDate = "N/A";

    protected override async Task OnInitializedAsync()
    {
        // TODO: Cargar estadísticas reales desde el servicio
        await LoadDashboardStats();
    }

    private async Task LoadDashboardStats()
    {
        try
        {
            // TODO: Implementar carga real de estadísticas
            // Por ahora, inicializar con valores vacíos
            await Task.CompletedTask;
        }
        catch (Exception)
        {
            // Manejar errores silenciosamente en el dashboard
        }
    }

    private string FormatNumber(long number)
    {
        if (number >= 1000000)
            return $"{number / 1000000.0:0.#}M";
        if (number >= 1000)
            return $"{number / 1000.0:0.#}K";
        return number.ToString();
    }

    private void NavigateToExtract()
    {
        Navigation.NavigateTo("/extract", forceLoad: false);
    }

    private void NavigateToExport()
    {
        Navigation.NavigateTo("/export", forceLoad: false);
    }

    private void NavigateToSettings()
    {
        Navigation.NavigateTo("/settings", forceLoad: false);
    }

    private void NavigateToHistory()
    {
        Navigation.NavigateTo("/history", forceLoad: false);
    }
}

<style>
    .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-top: 30px;
    }
    
    .card {
        background: linear-gradient(135deg, #0078D4 0%, #004578 100%);
        color: white;
        padding: 30px;
        border-radius: 15px;
        box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        transition: transform 0.3s, box-shadow 0.3s;
    }
    
    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 12px 35px rgba(0,0,0,0.25);
    }
    
    .card h2 {
        font-size: 56px;
        margin-bottom: 10px;
        font-weight: 700;
    }
    
    .card p {
        font-size: 18px;
        opacity: 0.95;
    }
    
    .btn {
        background: #0078D4;
        color: white;
        padding: 15px 30px;
        border: none;
        border-radius: 4px;
        font-size: 16px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s;
    }
    
    .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(0, 120, 212, 0.4);
        filter: brightness(1.1);
    }
</style>
